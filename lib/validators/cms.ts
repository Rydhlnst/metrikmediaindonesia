import { z } from "zod";

export const slugSchema = z.string().trim().min(2).max(150).regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/, "Use lowercase words separated by hyphens");
export const positiveIdSchema = z.coerce.number().int().positive();
export const entityTypeSchema = z.enum(["person", "organization", "place"]);
export const categorySchema = z.object({ name: z.string().trim().min(2).max(100), slug: slugSchema, description: z.string().trim().max(2_000).nullable().optional(), color: z.string().regex(/^#[0-9A-Fa-f]{6}$/).nullable().optional(), seoTitle: z.string().trim().max(255).nullable().optional(), seoDescription: z.string().trim().max(1_000).nullable().optional(), isActive: z.boolean().optional() });
export const tagSchema = z.object({ name: z.string().trim().min(2).max(100), slug: slugSchema });
export const topicSchema = z.object({ name: z.string().trim().min(2).max(100), slug: slugSchema, description: z.string().trim().max(2_000).nullable().optional(), cover: z.string().url().max(2_000).nullable().optional(), isActive: z.boolean().optional(), seoTitle: z.string().trim().max(255).nullable().optional(), seoDescription: z.string().trim().max(1_000).nullable().optional() });
export const locationSchema = z.object({ name: z.string().trim().min(2).max(100), slug: slugSchema, level: z.enum(["country", "province", "city", "district"]), parentId: z.coerce.number().int().positive().nullable().optional(), description: z.string().trim().max(2_000).nullable().optional(), isActive: z.boolean().optional() });
export const entityMetadataSchema = z.object({ position: z.string().trim().max(255).optional(), industry: z.string().trim().max(255).optional(), location: z.string().trim().max(255).optional(), website: z.string().url().max(2_000).optional() }).nullable().optional();
export const entitySchema = z.object({ name: z.string().trim().min(2).max(150), slug: slugSchema, type: entityTypeSchema, avatarOrLogo: z.string().url().max(2_000).nullable().optional(), bioOrDesc: z.string().trim().max(10_000).nullable().optional(), metadata: entityMetadataSchema, isActive: z.boolean().optional() });
export const authorSocialLinksSchema = z.object({ twitter: z.string().trim().max(500).optional(), instagram: z.string().trim().max(500).optional(), linkedin: z.string().trim().max(500).optional(), facebook: z.string().trim().max(500).optional() }).nullable().optional();
export const authorSchema = z.object({ name: z.string().trim().min(2).max(100), slug: slugSchema, bio: z.string().trim().max(10_000).nullable().optional(), avatar: z.string().trim().max(2_000).nullable().optional(), role: z.string().trim().min(2).max(50).optional(), socialLinks: authorSocialLinksSchema });
export const commentCreateSchema = z.object({ articleId: positiveIdSchema, content: z.string().trim().min(2).max(10_000), authorName: z.string().trim().min(2).max(100).nullable().optional(), authorEmail: z.string().trim().email().max(255).nullable().optional(), parentId: positiveIdSchema.nullable().optional() });
export const commentStatusSchema = z.enum(["approved", "pending", "spam", "rejected"]);
export const commentUpdateSchema = z.object({ status: commentStatusSchema });
export const pageSchema = z.object({ title: z.string().trim().min(2).max(255), slug: slugSchema, content: z.string().max(100_000).nullable().optional(), excerpt: z.string().trim().max(2_000).nullable().optional(), status: z.enum(["draft", "published", "archived"]).optional(), seoTitle: z.string().trim().max(255).nullable().optional(), seoDescription: z.string().trim().max(1_000).nullable().optional() });
export const roleSchema = z.object({ name: z.string().trim().min(2).max(50), description: z.string().trim().max(2_000).nullable().optional(), permissionKeys: z.array(z.string().trim().min(1).max(120)).max(100).optional() });
export const profileUpdateSchema = z.object({ name: z.string().trim().min(2).max(100).optional(), bio: z.string().trim().max(10_000).nullable().optional(), avatar: z.string().trim().max(2_000).nullable().optional(), socialLinks: authorSocialLinksSchema });
export const settingsSchema = z.record(z.string().trim().min(1).max(100), z.string().max(10_000)).refine((value) => Object.keys(value).length > 0, "At least one setting is required");
